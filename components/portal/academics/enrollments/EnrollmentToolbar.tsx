import { PageHeader } from "@/components/ui";

export default function EnrollmentToolbar() {
  return (
    <PageHeader
      title="Student Enrollment"
      description="Enroll students into classes for an academic session."
    />
  );
}
